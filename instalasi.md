# 🧠 Tutorial Setup Puppeteer + TypeScript Project

## ⚙️ Langkah 1: Instalasi Awal

Jalankan perintah berikut di terminal untuk menginstal semua dependensi yang dibutuhkan:

```bash
npm init -y
npm install puppeteer
npm install -D typescript ts-node @types/node
npm install


# Dev
npm run dev

# Build
npm run build

# Production
npm run prod